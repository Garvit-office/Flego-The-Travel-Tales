# Copy to .env.local and adjust as needed.

# Secret used to sign/verify JWTs — replace with a long random string in production
JWT_SECRET=flego_secret_key

# How long issued tokens stay valid (jsonwebtoken "expiresIn" format)
JWT_EXPIRES_IN=7d
